import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const MainBox = styled(Box)({});
export const ServicesItemWrapper = styled("div")((props) => ({
  backgroundColor: "#F6F6F6",
  padding: "40px",
}));
